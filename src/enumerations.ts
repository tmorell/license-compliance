export enum Formatter {
    csv = "Csv",
    json = "Json",
    text = "Text",
    xunit = "Xunit",
}

export enum LicenseStatus {
    valid = "valid",
    custom = "custom",
    unknown = "unknown",
}

export enum Literals {
    UNKNOWN = "UNKNOWN",
    CUSTOM = "CUSTOM",
}

export enum Report {
    detailed = "Detailed",
    summary = "Summary",
}
