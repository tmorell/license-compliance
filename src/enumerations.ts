export enum Formatter {
    csv = "Csv",
    json = "Json",
    text = "Text"
}

export enum LicenseStatus {
    valid = "valid",
    custom = "custom",
    unknown = "unknown"
}

export enum Literals {
    UNKNOWN = "UNKNOWN",
    CUSTOM = "CUSTOM"
}

export enum Report {
    detailed = "Detailed",
    invalid = "Invalid",
    summary = "Summary"
}
