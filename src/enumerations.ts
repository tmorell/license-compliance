export enum Format {
    csv = "csv",
    json = "json",
    text = "text",
    xunit = "xunit",
}

export enum LicenseStatus {
    valid = "valid",
    custom = "custom",
    unknown = "unknown",
}

export enum Literals {
    UNKNOWN = "UNKNOWN",
    CUSTOM = "CUSTOM",
}

export enum Report {
    detailed = "detailed",
    summary = "summary",
}
