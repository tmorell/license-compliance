export enum Formatter {
    csv = "Csv",
    json = "Json",
    text = "Text"
}

export enum LicenseStatus {
    valid = "valid",
    custom = "custom",
    unknown = "unknown"
}

export enum Report {
    detailed = "Detailed",
    invalid = "Invalid",
    summary = "Summary"
}
